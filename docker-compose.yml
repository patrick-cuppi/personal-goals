name: personal-goals

services:
  pg:
    image: bitnami/postgresql:13.16.0
    ports:
      - "3001:3001"
    environment:
      - POSTGRES_USER=docker
      - POSTGRES_PASSWORD=docker
      - POSTGRES_DB=personalGoals
